# Newspic Auto-Blogger (Threads + SEO) Master Guide

## 1. 프로젝트 핵심 요약 (비즈니스 로직)
- **최종 목표:** 뉴스픽 파트너스(Newspic Partners) 제휴 링크 수익 자동화
- **트래픽 전략 (Two-Track):**
  1. **단기 트래픽 (SNS):** 스레드(Threads)에 호기심 유발(어그로) 게시글을 작성하여 블로그로 유입.
  2. **장기 트래픽 (SEO):** 구글/네이버 검색 로봇의 '유사문서(표절)' 필터링을 피하기 위해 AI로 완전히 새로 작성된 장문 기사를 블로그에 적재.

## 2. 블로그 UI/UX (블러 및 버튼)
- 스레드와 블로그의 이탈률을 최소화하기 위해 **'가림막(Blur/Fade-out)' 효과**를 사용.
- **상단 (보이는 부분):** 도파민을 유발하는 3줄짜리 짧은 커뮤니티식 후킹 텍스트.
- **하단 (가려진 부분):** 검색 로봇(SEO)만을 위한 1,000자짜리 상세 뉴스 텍스트. 
- **액션 유도:** 블러 바로 아래에 거대한 `[👉 기사 원문 계속 읽기]` 버튼을 배치하여 뉴스픽 제휴 링크로 강제 랜딩 유도 (불법적인 0.1초 자동 리다이렉트가 아닌, 합법적 쿠션 페이지).

## 3. 뉴스픽 크롤링 백도어 (로그인 우회)
- 카카오 로그인이나 2FA 인증을 거쳐 파트너스 대시보드에 접근할 필요가 없음.
- 뉴스픽 메인 사이트(newspic.kr)에서 공개된 핫이슈 기사의 `nid`만 추출.
- 제휴 링크 공식: `https://www.newspic.kr/view.html?nid={기사번호}&pn={내파트너스ID(7440c8)}`
- 위 링크로 보내면 조회수 수익이 100% 정상 카운트됨.

## 4. 제미나이(AI) 세팅 및 순환 규칙
- **API Key:** `GEMINI_API_KEY` 하나를 깃허브 시크릿에 저장하여 사용.
- **프롬프트 분할 (다중인격):**
  - Hook: 커뮤니티나 유튜브 쇼츠 스크립트처럼 자극적이고 뒷내용이 궁금하게 작성.
  - SEO Body: 구글 로봇이 좋아할 만한 딱딱하고 전문적인 500자 이상의 긴 텍스트.
- **🚨 500개 한도 우회 룰 (필수):** API 호출 시 무조건 `gemini-3.5-flash-lite`와 `gemini-3.1-flash-lite` 2개를 리스트업 해두고, 429(한도초과) 에러 시 다음 모델로 순환(Rotation)하는 로직을 반드시 포함할 것.

## 5. 인프라 및 스케줄
- **저장소:** `somsoo/newspic-blog` (GitHub Pages 연동)
- **자동화:** GitHub Actions 워크플로우를 통해 **2시간 주기(cron: '0 */2 * * *')**로 실행.
- **포스팅 방식:** 기사별 고유 NID를 활용해 개별 HTML(`article_{nid}.html`) 무한 생성 (이전 스레드 링크가 끊기지 않게 보존).

## 6. 개발자 행동 수칙 (AI 지침)
- **묻지마 실행:** 코드 수정, 봇 업데이트 등 시스템 변경 시 유저의 허락을 구하는 핑퐁(대화)을 하지 말고 즉각 코드를 짠 뒤 터미널/깃허브에 푸시하여 결과를 통보할 것.
